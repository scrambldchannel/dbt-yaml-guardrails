# https://pre-commit.com/#creating-new-hooks — required at repo root when this
# project is used as a pre-commit `repo:` source (local path, git URL, etc.).

- id: model-allowed-keys
  name: dbt model allowed keys
  description: Validate top-level keys on each model entry in dbt property YAML
  entry: model-allowed-keys
  language: python
  types: [yaml]

- id: model-allowed-config-keys
  name: dbt model allowed config keys
  description: Validate top-level keys under config on each model entry in dbt property YAML
  entry: model-allowed-config-keys
  language: python
  types: [yaml]

- id: macro-allowed-config-keys
  name: dbt macro allowed config keys
  description: Validate top-level keys under config on each macro entry in dbt property YAML
  entry: macro-allowed-config-keys
  language: python
  types: [yaml]

- id: seed-allowed-config-keys
  name: dbt seed allowed config keys
  description: Validate top-level keys under config on each seed entry in dbt property YAML
  entry: seed-allowed-config-keys
  language: python
  types: [yaml]

- id: source-allowed-config-keys
  name: dbt source allowed config keys
  description: Validate top-level keys under config on each source entry in dbt property YAML
  entry: source-allowed-config-keys
  language: python
  types: [yaml]

- id: snapshot-allowed-config-keys
  name: dbt snapshot allowed config keys
  description: Validate top-level keys under config on each snapshot entry in dbt property YAML
  entry: snapshot-allowed-config-keys
  language: python
  types: [yaml]

- id: exposure-allowed-config-keys
  name: dbt exposure allowed config keys
  description: Validate top-level keys under config on each exposure entry in dbt property YAML
  entry: exposure-allowed-config-keys
  language: python
  types: [yaml]

- id: macro-allowed-keys
  name: dbt macro allowed keys
  description: Validate top-level keys on each macro entry in dbt property YAML
  entry: macro-allowed-keys
  language: python
  types: [yaml]

- id: seed-allowed-keys
  name: dbt seed allowed keys
  description: Validate top-level keys on each seed entry in dbt property YAML
  entry: seed-allowed-keys
  language: python
  types: [yaml]

- id: source-allowed-keys
  name: dbt source allowed keys
  description: Validate top-level keys on each source entry in dbt property YAML
  entry: source-allowed-keys
  language: python
  types: [yaml]

- id: snapshot-allowed-keys
  name: dbt snapshot allowed keys
  description: Validate top-level keys on each snapshot entry in dbt property YAML
  entry: snapshot-allowed-keys
  language: python
  types: [yaml]

- id: exposure-allowed-keys
  name: dbt exposure allowed keys
  description: Validate top-level keys on each exposure entry in dbt property YAML
  entry: exposure-allowed-keys
  language: python
  types: [yaml]

- id: model-allowed-meta-keys
  name: dbt model allowed meta keys
  description: Validate keys on config.meta for each model entry in dbt property YAML
  entry: model-allowed-meta-keys
  language: python
  types: [yaml]

- id: seed-allowed-meta-keys
  name: dbt seed allowed meta keys
  description: Validate keys on config.meta for each seed entry in dbt property YAML
  entry: seed-allowed-meta-keys
  language: python
  types: [yaml]

- id: snapshot-allowed-meta-keys
  name: dbt snapshot allowed meta keys
  description: Validate keys on config.meta for each snapshot entry in dbt property YAML
  entry: snapshot-allowed-meta-keys
  language: python
  types: [yaml]

- id: exposure-allowed-meta-keys
  name: dbt exposure allowed meta keys
  description: Validate keys on config.meta for each exposure entry in dbt property YAML
  entry: exposure-allowed-meta-keys
  language: python
  types: [yaml]

- id: source-allowed-meta-keys
  name: dbt source allowed meta keys
  description: Validate keys on config.meta for each source entry in dbt property YAML
  entry: source-allowed-meta-keys
  language: python
  types: [yaml]

- id: macro-allowed-meta-keys
  name: dbt macro allowed meta keys
  description: Validate keys on config.meta for each macro entry in dbt property YAML
  entry: macro-allowed-meta-keys
  language: python
  types: [yaml]

- id: model-meta-accepted-values
  name: dbt model meta accepted values
  description: Validate a string or string list under config.meta against a comma-separated allowlist (dot path via --key)
  entry: model-meta-accepted-values
  language: python
  types: [yaml]

- id: seed-meta-accepted-values
  name: dbt seed meta accepted values
  description: Validate a string leaf under config.meta against a comma-separated allowlist (dot path via --key)
  entry: seed-meta-accepted-values
  language: python
  types: [yaml]

- id: snapshot-meta-accepted-values
  name: dbt snapshot meta accepted values
  description: Validate a string or string list under config.meta against a comma-separated allowlist (dot path via --key)
  entry: snapshot-meta-accepted-values
  language: python
  types: [yaml]

- id: exposure-meta-accepted-values
  name: dbt exposure meta accepted values
  description: Validate a string or string list under config.meta against a comma-separated allowlist (dot path via --key)
  entry: exposure-meta-accepted-values
  language: python
  types: [yaml]

- id: source-meta-accepted-values
  name: dbt source meta accepted values
  description: Validate a string or string list under config.meta against a comma-separated allowlist (dot path via --key)
  entry: source-meta-accepted-values
  language: python
  types: [yaml]

- id: macro-meta-accepted-values
  name: dbt macro meta accepted values
  description: Validate a string or string list under config.meta against a comma-separated allowlist (dot path via --key)
  entry: macro-meta-accepted-values
  language: python
  types: [yaml]

- id: model-tags-accepted-values
  name: dbt model tags accepted values
  description: Validate config.tags values against a comma-separated allowlist (--values)
  entry: model-tags-accepted-values
  language: python
  types: [yaml]

- id: seed-tags-accepted-values
  name: dbt seed tags accepted values
  description: Validate config.tags values against a comma-separated allowlist (--values)
  entry: seed-tags-accepted-values
  language: python
  types: [yaml]

- id: snapshot-tags-accepted-values
  name: dbt snapshot tags accepted values
  description: Validate config.tags values against a comma-separated allowlist (--values)
  entry: snapshot-tags-accepted-values
  language: python
  types: [yaml]

- id: exposure-tags-accepted-values
  name: dbt exposure tags accepted values
  description: Validate config.tags values against a comma-separated allowlist (--values)
  entry: exposure-tags-accepted-values
  language: python
  types: [yaml]

- id: source-tags-accepted-values
  name: dbt source tags accepted values
  description: Validate config.tags values against a comma-separated allowlist (--values)
  entry: source-tags-accepted-values
  language: python
  types: [yaml]

- id: macro-tags-accepted-values
  name: dbt macro tags accepted values
  description: Validate config.tags values against a comma-separated allowlist (--values)
  entry: macro-tags-accepted-values
  language: python
  types: [yaml]
